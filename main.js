
// Define what you export as a variable (constant) to be used in this file.
const apple = require('./apple.js');
const banana = require('./banana.js');
const coconut = require('./api/coconut.js');

// console.log(apple() + " are red.");
// console.log(banana() + " are yellow.");
// console.log(coconut() + " are white on the inside.");

const aa = apple() + " are red.";

console.log(aa);